# Docker Compose / runtime
FRONTEND_PORT=8080
BACKEND_PORT=3001
HOST=0.0.0.0
PORT=3001

# If your runtime that executes YAML instructions calls OpenAI
OPENAI_API_KEY=

# Optional paths used by aigen Python runtime
CONFIGS_ROOT_DIR=
AIGEN_CACHE_DIR=
